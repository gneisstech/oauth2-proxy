apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cf-oauth-proxy-docker.fullname" . }}-test-connection"
  labels:
    {{- include "cf-oauth-proxy-docker.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cf-oauth-proxy-docker.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
