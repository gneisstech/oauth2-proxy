apiVersion: v1
kind: Service
metadata:
  name: {{ include "cf-oauth-proxy-docker.fullname" . }}
  labels:
    {{- include "cf-oauth-proxy-docker.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cf-oauth-proxy-docker.selectorLabels" . | nindent 4 }}
