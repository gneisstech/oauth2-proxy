{{- if .Values.config.environment }}
apiVersion: v1
kind: ConfigMap
metadata:
  labels:
    app: {{ template "cf-oauth-proxy-docker.name" . }}
    chart: {{ template "cf-oauth-proxy-docker.chart" . }}
    component: "{{ template "cf-oauth-proxy-docker.name" . }}"
    heritage: {{ .Release.Service }}
    release: {{ .Release.Name }}
  name: {{ template "cf-oauth-proxy-docker.fullname" . }}-env
data:
{{ tpl (toYaml .Values.config.environment) . | indent 2 }}
{{- end }}
