apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "br-oauth-proxy-docker.fullname" . }}-test-connection"
  labels:
    {{- include "br-oauth-proxy-docker.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "br-oauth-proxy-docker.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
