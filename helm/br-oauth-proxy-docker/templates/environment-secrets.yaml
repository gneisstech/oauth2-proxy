{{- if .Values.config.secrets }}
apiVersion: v1
kind: Secret
metadata:
  labels:
    app: {{ template "br-oauth-proxy-docker.name" . }}
    chart: {{ template "br-oauth-proxy-docker.chart" . }}
    component: "{{ template "br-oauth-proxy-docker.name" . }}"
    heritage: {{ .Release.Service }}
    release: {{ .Release.Name }}
  name: {{ template "br-oauth-proxy-docker.fullname" . }}-senv
  annotations:
    checksum/secrets: {{ tpl (toYaml .Values.config.secrets) . | sha256sum }}
data:
  {{- range $key, $value := .Values.config.secrets }}
  {{ $key }}: {{ $value | b64enc | quote }}
  {{- end }}
{{- end }}
