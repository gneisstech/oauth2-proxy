apiVersion: v1
kind: Service
metadata:
  name: {{ include "br-oauth-proxy-docker.fullname" . }}
  labels:
    {{- include "br-oauth-proxy-docker.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "br-oauth-proxy-docker.selectorLabels" . | nindent 4 }}
